# pnpm >= 10 blocks dependency build scripts by default. These two compile or
# fetch native binaries and genuinely need theirs:
#   esbuild       - bundler used by Vite, which powers Storybook
#   unrs-resolver - native resolver behind eslint-plugin-import
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
